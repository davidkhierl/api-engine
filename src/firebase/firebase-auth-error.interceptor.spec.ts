import { FirebaseAuthErrorInterceptor } from './firebase-auth-error.interceptor';

describe('FirebaseAuthErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new FirebaseAuthErrorInterceptor()).toBeDefined();
  });
});
